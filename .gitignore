.env
*.tgz
requirements.lock

mounts/

# Certificates generated by generate-certs.sh, including private keys.
certs/

# Python tooling for the tests. uv.lock is committed, so the test run is
# reproducible; the environment it builds is not.
.venv/
__pycache__/
.pytest_cache/
.ruff_cache/

